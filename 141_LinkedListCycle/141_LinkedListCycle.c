/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
bool hasCycle(struct ListNode *head) {
    /*int visited = -1;
    int result;
    int count = 0;
    result = hcRecur(head->next,visited,count);
    if(result>0)return true;
    return false;
    */
    struct ListNode *fast
}

/*int hcRecur(struct ListNode *head,int visited,int count){
    visited *= -1;
    count +=1;
    if(!(head->next)){
        return -1;
    }else{
        if(count%2==0){
            if(visited==-1)return  1;
        }else{
            if(visited==1)return 1;
        }
    }
    hasCycleRecur(head->next,visited,count);
    
}*/
